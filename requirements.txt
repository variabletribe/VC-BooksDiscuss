python-telegram-bot[job-queue,webhooks]>=21.0
python-dotenv>=1.0.0
pymongo>=4.7
telethon>=1.36.0
httpx>=0.27.0

# Optional: lets the Telethon assistant account actually JOIN a group's voice/
# video chat (vc_autojoin.py), not just observe the participant list. Needs an
# ffmpeg binary on PATH at runtime too — see render.yaml's buildCommand. If
# this fails to build/import, everything else (VC time-tracking, /mystats,
# etc.) keeps working unaffected; set VC_AUTOJOIN_DISABLE=1 to turn it off
# without removing the dependency.
py-tgcalls[telethon]==2.3.3
